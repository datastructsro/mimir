<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Category Overrides -->
    <record id="mimir_category_override_view_list" model="ir.ui.view">
        <field name="name">mimir.category.override.list</field>
        <field name="model">mimir.category.override</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="category_id"/>
                <field name="service_level"/>
                <field name="safety_factor"/>
                <field name="lead_time_override_days"/>
                <field name="review_period_override_days"/>
                <field name="default_min_qty" optional="hide"/>
                <field name="default_max_qty" optional="hide"/>
                <field name="excluded"/>
            </list>
        </field>
    </record>

    <record id="mimir_category_override_action" model="ir.actions.act_window">
        <field name="name">Category Overrides</field>
        <field name="res_model">mimir.category.override</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Product Overrides -->
    <record id="mimir_product_override_view_list" model="ir.ui.view">
        <field name="name">mimir.product.override.list</field>
        <field name="model">mimir.product.override</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="product_id"/>
                <field name="service_level"/>
                <field name="safety_factor"/>
                <field name="min_qty_floor"/>
                <field name="max_qty_ceiling"/>
                <field name="lead_time_override_days"/>
                <field name="excluded"/>
            </list>
        </field>
    </record>

    <record id="mimir_product_override_action" model="ir.actions.act_window">
        <field name="name">Product Overrides</field>
        <field name="res_model">mimir.product.override</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menus -->
    <menuitem id="mimir_menu_overrides" name="Overrides" parent="mimir_menu_root" sequence="30"/>
    <menuitem id="mimir_menu_category_override" name="Category Overrides" parent="mimir_menu_overrides" action="mimir_category_override_action" sequence="10"/>
    <menuitem id="mimir_menu_product_override" name="Product Overrides" parent="mimir_menu_overrides" action="mimir_product_override_action" sequence="20"/>
</odoo>
